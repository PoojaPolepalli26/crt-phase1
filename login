def login():
    n=1
    while n!=0:
        a=input("enter the name")
        b=input("enter password")
        if a==b:
            
            print("login is successful")
            break

        else:
            print("incorrect")
login()
    
